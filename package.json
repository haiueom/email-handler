{
	"name": "email-handler",
	"version": "1.0.0",
	"scripts": {
		"deploy": "wrangler deploy",
		"deploy-min": "wrangler deploy --minify",
		"dev": "wrangler dev",
		"cf-typegen": "wrangler types"
	},
	"dependencies": {
		"cheerio": "^1.1.0",
		"postal-mime": "^2.4.4"
	},
	"devDependencies": {
		"typescript": "^5.8.3",
		"wrangler": "^4.20.0"
	}
}
